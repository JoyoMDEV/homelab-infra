---
apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: homelab-pg
  namespace: infrastructure
spec:
  instances: 1

  postgresql:
    parameters:
      shared_buffers: "256MB"
      effective_cache_size: "512MB"
      max_connections: "100"

  storage:
    size: 10Gi

  resources:
    requests:
      memory: 512Mi
      cpu: 250m
    limits:
      memory: 1Gi

  bootstrap:
    initdb:
      database: homelab
      owner: homelab

  monitoring:
    # PodMonitor aktiviert – Prometheus sammelt CNPG-Metriken automatisch ein.
    # Voraussetzung: kube-prometheus-stack muss im Cluster laufen.
    # Dashboard-Import: Grafana → Import → ID 20417 (CloudNativePG)
    enablePodMonitor: true
