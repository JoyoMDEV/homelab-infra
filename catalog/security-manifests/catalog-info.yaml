apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: security-manifests
  description: Cluster security manifests (network policies, ExternalSecrets, Vault ClusterSecretStore)
  annotations:
    argocd/app-name: security-manifests
    backstage.io/kubernetes-id: security-manifests
    backstage.io/techdocs-ref: dir:.
spec:
  type: service
  lifecycle: production
  owner: group:homelab/johannes
  system: homelab
